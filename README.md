# dotfiles

Install packages:
```
./scripts/install-packages.sh
```

Install configs:
```
./install.sh
```

Notes:
- Quickshell settings are saved to `~/.config/floating-garden/config.json` (not tracked here).
